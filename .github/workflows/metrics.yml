name: Collect Metrics

on:
  push:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Setup Go
      uses: actions/setup-go@v2
      with:
          go-version: 1.15
    - name: Report metrics
      run: sh report.sh
